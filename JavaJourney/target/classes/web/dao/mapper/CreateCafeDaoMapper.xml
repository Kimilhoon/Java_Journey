<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="web.dao.face.CreateCafeDao">

	<insert id="write" parameterType="Cafe">
		<selectKey order="BEFORE" resultType="int" keyProperty="cafeNo">
			SELECT cafeNo_seq.nextval FROM dual
		</selectKey>
		INSERT INTO CAFE (cafeNo, imgNo, cafeName, cafeComm, cafePhone
		, busyTime, cafeLoc, cafeAdd1, cafeAdd2)
		VALUES (#{cafeNo}, #{imgNo}, #{cafeName}, #{cafeComm}, #{cafePhone}
		, #{busyTime}, #{cafeLoc}, #{cafeAdd1}, #{cafeAdd2})
	</insert>
	
	<select id="selectAllImg" resultType="Cafe">
		SELECT	cafeImgNo, cafeImgOriName, cafeImgStoName, cafeImgSize
		FROM CafeImg
		ORDER BY cafeImgNo
	</select>
	
	<update id="update" parameterType="Cafe">
		UPDATE CAFE
		<set>
			, cafeName = #{cafeName}
			, cafeComm = #{cafeComm}
			, busyTime = #{busyTime}
			, cafeInfo = #{cafeInfo}
			, cafeAdd1 = #{cafeAdd1}
			, cafeAdd2 = #{cafeAdd2}
		</set>
		<where>
			cafeNo = #{cafeNo}
		</where>
	</update>
	<!-- 질문. 주소는 지도로 찍는건데 update로 수정해줘야하는지
		우편번호는 dto에 없는 id="postCode"로 해뒀는데 어떻게 해야하는지 -->
		
	<delete id="delete">
		DELETE Cafe
		<where>
			cafeNo = #{cafeNo}
		</where>
	</delete>
</mapper>